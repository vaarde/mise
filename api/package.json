{
  "name": "mise-api",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc --noEmit",
    "bundle": "esbuild src/handlers/index.ts src/handlers/applyWorker.ts --bundle --platform=node --target=node22 --format=cjs --outdir=dist --sourcemap",
    "test": "tsx --test src/**/*.test.ts"
  },
  "dependencies": {
    "@aws-sdk/client-bedrock-agentcore": "^3.1111.0",
    "@aws-sdk/client-dynamodb": "^3.1111.0",
    "@aws-sdk/client-lambda": "^3.1111.0",
    "@aws-sdk/client-s3": "^3.1111.0",
    "@aws-sdk/client-secrets-manager": "^3.1111.0",
    "@aws-sdk/lib-dynamodb": "^3.1111.0"
  },
  "devDependencies": {
    "@types/aws-lambda": "^8.10.149",
    "@types/node": "^22.10.0",
    "esbuild": "^0.25.9",
    "tsx": "^4.19.2",
    "typescript": "^5.7.2"
  }
}
